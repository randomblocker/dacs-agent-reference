node_modules/
sdk
.next/
# operational secrets — never commit
.buyer-key
.buyer-evm-key
.env
.env.*
!.env.example
!**/*.env.example
*.key
*.mnemonic
*.pem
*.p12
*.pfx
*.keystore
.npmrc
# local artifacts
*.log
# L1 live pay-dem run report — real on-chain output, regenerated by `npm run dacs:l1`
roster/dacs/live/L1-run.log
# L1 persisted wallet keys (secrets) — dedicated seller/buyer, established across runs, never commit
roster/dacs/live/.l1-buyer-key
roster/dacs/live/.l1-seller-key
# L2 x402 run report — real on-chain output, regenerated by `npm run dacs:l2`
roster/dacs/live/L2-run.log
# L2 persisted EVM wallet keys (secrets) — buyer USDC payer + seller USDC payout, never commit
roster/dacs/live/.l2-buyer-evm-key
roster/dacs/live/.l2-seller-evm-key
# L3 Butler basket run report — real on-chain output, regenerated by `npm run dacs:l3`
roster/dacs/live/L3-run.log
# L3 persisted wallet keys (secrets) — dedicated dd-researcher seller + evalbot, never commit
roster/dacs/live/.l3-dd-seller-key
roster/dacs/live/.l3-evalbot-key
# L4 full-roster basket run report — real on-chain output, regenerated by `npm run dacs:l4`
roster/dacs/live/L4-run.log
# L4 persisted wallet keys (secrets) — dedicated per-seller DEM wallets, never commit
roster/dacs/live/.l4-depup-key
roster/dacs/live/.l4-lead-key
roster/dacs/live/.l4-treasury-key
roster/dacs/live/.l4-site-key
roster/dacs/live/.l4-sec-key
roster/dacs/live/.l4-compliance-key
.playwright-mcp/
# generated DD reports
roster/dd-researcher/out/
# dep-upgrade demo fixture — standalone npm package, its install artifacts are generated
roster/dep-upgrade/fixture/node_modules/
roster/dep-upgrade/fixture/package-lock.json
# evalbot ruling ledger — local track record, regenerated by the demo
roster/evalbot/out/
# treasury-ops run journals — local execution state, regenerated by the demo
roster/treasury-ops/out/
# site-auditor audit reports — local artifacts, regenerated by the demo
roster/site-auditor/out/
# sec-audit reports — local artifacts, regenerated by the demo
roster/sec-audit/out/
# Anthropic usage quota ledger — runtime state, never commit
roster/llm/out/
# Public demo run receipts — persisted runtime state, never commit
roster/gateway/out/
RELEASE-MANIFEST.sha256
runs.json
# compliance screening reports + list-body cache — local artifacts, regenerated by the demo
roster/compliance/out/

.DS_Store
.context/
